language: cpp

matrix:
  include:
    - os: linux
      compiler: gcc
      addons:
        apt:
          packages:
            - libbz2-dev
            - libcurl4-openssl-dev
            - libfribidi-dev
            - libgtk-3-dev
            - libjpeg-dev
            - libxml2-dev

    - os: linux
      compiler: clang
      addons:
        apt:
          packages:
            - libbz2-dev
            - libcurl4-openssl-dev
            - libfribidi-dev
            - libgtk-3-dev
            - libjpeg-dev
            - libxml2-dev

    - os: osx
      compiler: gcc

    - os: osx
      compiler: clang

script:
  - make
